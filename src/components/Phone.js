import React from 'react';
import styled from 'styled-components';
import colors from '../utils/colors';

export default () => (
  <Phone>809-573-0000</Phone>
);

const Phone = styled.span`
  font-size: 0.8rem;
`