import React from 'react';
import styled from 'styled-components';

export default () => (
  <Logo>
    ingse<span style={{color: '#FF7C00'}}>com</span>
  </Logo>
);

const Logo = styled.span`
  font-weight: 600;
  font-size: 1.625rem;
`